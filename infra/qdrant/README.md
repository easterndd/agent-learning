# Qdrant

Qdrant is the MVP vector store.

Initial collection design will be added during the RAG milestone. Expected payload fields:

- `knowledge_base_id`
- `document_id`
- `chunk_id`
- `source_file_name`
- `page_number`
- `chunk_index`
